---
import Projects from '../components/Projects.astro';
import Webring from '../components/Webring.astro';
import '../styles/global.css';

const pageTitle = "About Me";

const identity = {
  firstName: "xxxxxxxx",
  country: "Web",
  occupation: "Security Researcher",
//   hobbies: ["Reading Books", "Dancing", "Making Content"],
  course: ["Computer Science"]
};

const languages = ["JavaScript / Typescript","Python","Lua","Java (Scanty)"];
const frameworks = ["React","Astro","Flask"];
const interests = ["InfoSec","Reading Books", "Malware Research","Dubstep"]
const oses = ["Archlinux (Hyprland)","Ubuntu 24.04 (Hyprland)", "Windows 11 (eww)"]
const games = ["Apex Legends","Call of Duty","Mr Mine (Very Cool Game)","Football Manager","GTA"]
const skillColor = "white";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/ico" href="/logo.ico" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Iceland&display=swap" rel="stylesheet">
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} >
    <title>{pageTitle}</title>
    <style define:vars={{skillColor}}>
        /* Base styles for larger screens */
h1 {
    font-size: 2rem;
    font-weight: bold;
}

.skill {
    color: var(--skillColor);
}

.categories-container {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: space-between;
}

.category {
    flex: 1;
    min-width: 250px;
}

.category h2 {
    font-size: 1.2rem;
    margin-bottom: -14px;
    margin-left: 25px;
}

.category ul {
    list-style: none;
}

/* Media query for small screens (max-width: 640px) */
@media (max-width: 640px) {
    h1 {
        font-size: 1.5rem; /* Adjust font size for small screens */
    }

    .categories-container {
        flex-direction: column; /* Stack categories vertically on small screens */
        gap: 10px; /* Reduce gap between categories */
    }

    .category {
        min-width: auto; /* Remove the min-width restriction on categories */
        width: 100%; /* Allow categories to take full width */
    }

    .category h2 {
        font-size: 1rem; /* Adjust font size for category titles */
        margin-left: 10px; /* Reduce left margin */
    }

    ul {
        padding-left: 20px; /* Adjust padding for better spacing */
    }

    .text-xl {
        font-size: 1rem; /* Reduce text size for better readability */
    }
}

/* Media query for medium screens (max-width: 768px) */
@media (max-width: 768px) {
    h1 {
        font-size: 1.75rem; /* Slightly larger font size for tablets */
    }

    .categories-container {
        flex-direction: column; /* Stack categories vertically */
        gap: 15px; /* Moderate gap between categories */
    }

    .category h2 {
        font-size: 1.1rem; /* Adjust category title font size */
    }
}

/* Media query for large screens (max-width: 1024px) */
@media (max-width: 1024px) {
    h1 {
        font-size: 1.8rem; /* Adjust font size for larger tablets or small laptops */
    }

    .categories-container {
        flex-direction: row; /* Keep categories in a row */
        gap: 20px; /* Standard gap between categories */
    }

    .category {
        flex: 1; /* Ensure each category takes equal space */
    }

    .category h2 {
        font-size: 1.2rem; /* Adjust font size for category titles */
    }
}

    </style>
  </head>
  <body >
    <div class="text-white text-xl ml-10 font-iceland">
      <p><a href="/"><i class="fas fa-arrow-left text-custom-green hover:text-white"> Home</i></a></p>
      <h1 class="text-purple-500">{pageTitle}</h1>
    <ul>
        <li>I <span class="text-purple-600">live</span> on the <span class="text-purple-600">web</span>.</li>
        <li>I <span class="text-purple-600">broke</span> computers alot as a kid and that's when I <span class="text-purple-600">found</span> my <span class="text-purple-600">passion</span></li>
        <li><span class="text-purple-600">Interacting</span> with randos on the <span class="text-purple-600">web</span> is kinda the norm for me</li>
        <li>I <span class="text-purple-600">hate</span> big tech companies with my whole <span class="text-purple-600">dark</span> soul</li>
        <li>I love <span class="text-purple-600">networking</span> and the world of <span class="text-purple-600">CyberSecurity</span></li>
    </ul>

    <div class="categories-container">
      <div class="category">
          <h2 class="text-purple-500">Languages & Tools</h2>
          <ul>
              {languages.map((language) => <li>{language}</li>)}
          </ul>
      </div>
      <div class="category">
          <h2 class="text-purple-500">Web Frameworks</h2>
          <ul>
              {frameworks.map((framework) => <li>{framework}</li>)}
          </ul>
      </div>
      <div class="category">
          <h2 class="text-purple-500">Interests</h2>
          <ul>
              {interests.map((interest) => <li>{interest}</li>)}
          </ul>
      </div>
      <div class="category">
          <h2 class="text-purple-500">Operating Systems and Setup</h2>
          <ul>
              {oses.map((os) => <li>{os}</li>)}
          </ul>
      </div>
      <div class="category">
        <h2 class="text-purple-500">Games I play / Have Finished (Not the full list)</h2>
        <ul>
            {games.map((game) => <li>{game}</li>)}
        </ul>
    </div>
  </div>
    <h2 class="text-purple-500">Some of the stuff I built:</h2>
    <Projects />
    <p>This site will update as I add stuff, so keep checking back</p>
    </div>  
    <Webring />
    <script>
      import "../scripts/menu.js";
    </script>
  </body>
</html>